# =========================
# AxAbsEnt – GPU Requirements
# =========================

# --- Core GPU Stack ---
cupy-cuda12x==12.2.0              # NVIDIA CUDA 12.x (Change for version alignment)
torch==2.2.1+cu121                # PyTorch w/ CUDA 12.1
torchvision==0.17.1+cu121
torchaudio==2.2.1+cu121
jax[cuda12_pip]==0.4.25           # JAX for GPU tensors

# --- GPU Math & Tensor Libraries ---
tensorly==0.8.1
opt_einsum==3.3.0
einops==0.7.0
scikit-cuda==0.5.4
cuquantum-python==23.10.0         # NVIDIA’s GPU quantum simulation backend

# --- RAPIDS AI (GPU DataFrame, ML, Stats) ---
cudf-cu12==24.02.00
cuml-cu12==24.02.00
cugraph-cu12==24.02.00
cuproj-cu12==24.02.00

# --- HPC / Multi-GPU Runtime ---
nccl==2.19.3                      # NVIDIA Collective Communications Library
mpi4py==3.1.6                     # Multi-node parallelism
ray[default]==2.10.0              # Optional distributed GPU execution

# --- Simulation + ML ---
numba==0.59.0
optuna==3.6.1                     # Auto-tuning and GPU hyperparameter search
scikit-learn==1.4.1
xgboost==2.0.3                    # Optional GPU-accelerated boosting

# --- Visualization with GPU-Backends ---
pyvista==0.43.3
plotly==5.21.0
matplotlib==3.8.3
kaleido==0.2.1
ipywidgets==8.1.2

# --- Scientific Logging / Monitoring ---
wandb==0.16.4                     # GPU usage tracking + dashboard
tensorboard==2.16.2
loguru==0.7.2

# --- Backend APIs ---
fastapi==0.110.0
uvicorn[standard]==0.29.0
pydantic==2.6.3

# --- Security + Profiling ---
python-dotenv==1.0.1
pynvml==11.5.0                    # NVIDIA GPU diagnostics
gpustat==1.1.1

# --- Jupyter & Notebooks (GPU-aware) ---
jupyterlab==4.1.2
notebook==7.1.1
ipykernel==6.29.3
nbconvert==7.16.2

# --- Optional Acceleration Extensions ---
nvidia-tensorrt==8.6.1            # Optional: Inference optimization
onnxruntime-gpu==1.17.1           # Optional: GPU-accelerated ONNX runtime

# =========================
# Notes:
# - Use with NVIDIA Driver >= 535 and CUDA Toolkit >= 12.2
# - Ensure virtual environment or container has proper GPU access
# =========================
